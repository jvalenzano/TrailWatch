---
description: Scaffolds a flat workspace structure with .agent rules and workflows
---

1. Create directory structure:
   - `src/` (Source code)
   - `tests/` (Tests)
   - `scripts/` (Dev scripts)
   - `.agent/workflows/` (Agent workflows)

2. Create basic files:
   - `src/trailwatch/__init__.py`
   - `tests/__init__.py`
   - `.env.example`
   - `requirements.txt`
   - `pyproject.toml`

3. Initialize Git (ensure proper .gitignore)

4. Initialize Gemini Conductor
